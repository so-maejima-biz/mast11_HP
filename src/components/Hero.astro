---
---

<section class="hero">
  <div class="hero-bg">
    <img
      src="/images/HP TOP PICTURE.png"
      alt="OUR WORKS - Planning & Design, Development, Production"
      class="hero-bg-image"
      loading="eager"
    />
  </div>
  <div class="hero-overlay"></div>
  <div class="container hero-content">
    <p class="hero-eyebrow" data-animate>Total produce of Footwear products.</p>
    <h1 class="hero-title" data-animate data-animate-delay="1">
      理想を設計し、<br />品質を形にする。
    </h1>
    <p class="hero-subtitle" data-animate data-animate-delay="2">
      企画から生産まで一貫して手がける、<br class="br-sp" />
      モノづくりのプロフェッショナル集団
    </p>
    <div class="hero-cta" data-animate data-animate-delay="3">
      <a href="#contact" class="btn btn-primary btn-lg">お問い合わせ</a>
      <a href="#about" class="btn btn-outline btn-lg">詳しく見る</a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: auto;
    display: block;
    padding-top: 72px;
    overflow: hidden;
    background-color: var(--color-primary);
  }

  .hero-bg {
    position: relative;
    width: 100%;
    z-index: 1;
  }

  .hero-bg-image {
    width: 100%;
    height: auto;
    display: block;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    top: 72px;
    z-index: 2;
    background: linear-gradient(135deg, rgba(0, 123, 167, 0.75) 0%, rgba(0, 77, 106, 0.8) 100%);
  }

  .hero-content {
    position: absolute;
    top: 72px;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    z-index: 3;
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-align: center;
    padding: var(--spacing-3xl);
    width: calc(100% - var(--spacing-3xl) * 2);
    max-width: 800px;
    box-sizing: border-box;
  }

  .hero-eyebrow {
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-medium);
    color: rgba(255, 255, 255, 0.85);
    letter-spacing: 0.2em;
    margin-bottom: var(--spacing-lg);
  }

  .hero-title {
    font-size: clamp(2.5rem, 8vw, 4.5rem);
    font-weight: var(--font-weight-bold);
    color: var(--color-bg);
    margin-bottom: var(--spacing-xl);
    letter-spacing: 0.02em;
    line-height: 1.4;
  }

  .hero-subtitle {
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    color: rgba(255, 255, 255, 0.85);
    margin-bottom: var(--spacing-2xl);
    line-height: var(--line-height-relaxed);
  }

  .br-sp {
    display: none;
  }

  .hero-cta {
    display: flex;
    gap: var(--spacing-md);
    flex-wrap: wrap;
  }

  .hero-cta .btn-primary {
    background-color: var(--color-bg);
    color: var(--color-primary);
    border-color: var(--color-bg);
  }

  .hero-cta .btn-primary:hover {
    background-color: rgba(255, 255, 255, 0.9);
    border-color: rgba(255, 255, 255, 0.9);
  }

  .hero-cta .btn-outline {
    color: var(--color-bg);
    border-color: rgba(255, 255, 255, 0.5);
  }

  .hero-cta .btn-outline:hover {
    background-color: rgba(255, 255, 255, 0.1);
    border-color: var(--color-bg);
  }

  @media (max-width: 768px) {
    .hero {
      min-height: 100svh;
    }

    .hero-content {
      left: 0;
      transform: none;
      width: 100%;
      text-align: center;
      max-width: 100%;
      padding: var(--spacing-2xl) var(--spacing-lg) var(--spacing-lg);
      justify-content: flex-start;
    }

    .hero-title {
      font-size: clamp(1.75rem, 6vw, 2.5rem);
    }

    .br-sp {
      display: block;
    }

    .hero-cta {
      justify-content: center;
      flex-direction: column;
      align-items: center;
    }

    .hero-cta .btn {
      width: 100%;
      max-width: 280px;
    }
  }
</style>
