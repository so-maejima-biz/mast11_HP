---
const services = [
  {
    number: '01',
    title: '企画・コンセプト',
    description: 'マーケットリサーチから商品コンセプトの策定まで、ブランドの世界観を形にします。',
  },
  {
    number: '02',
    title: 'ラスト・ソール設計',
    description: '履き心地と機能性を追求した、独自のラスト設計とソール開発を行います。',
  },
  {
    number: '03',
    title: 'アッパーデザイン',
    description: 'トレンドと機能性を両立したアッパーデザインで、商品価値を高めます。',
  },
  {
    number: '04',
    title: '生産・品質管理',
    description: '信頼できる協力工場と連携し、細部までこだわった品質管理を実践します。',
  },
];
---

<section class="service" id="service">
  <div class="container">
    <div class="service-header" data-animate>
      <span class="service-label">SERVICE</span>
      <h2 class="service-heading">一貫したものづくりで、<br class="br-sp" />価値を創造する</h2>
    </div>
    <div class="service-grid">
      {services.map((service, index) => (
        <div class="service-card" data-animate data-animate-delay={index + 1}>
          <span class="service-number">{service.number}</span>
          <h3 class="service-title">{service.title}</h3>
          <p class="service-description">{service.description}</p>
        </div>
      ))}
    </div>
    <div class="service-cta">
      <p class="service-cta-text">
        OEM・ODMのご相談も承ります。まずはお気軽にお問い合わせください。
      </p>
      <a href="#contact" class="btn btn-primary">お問い合わせ</a>
    </div>
  </div>
</section>

<style>
  .service {
    padding: var(--spacing-4xl) 0;
    background: var(--color-bg-alt);
  }

  .service-header {
    text-align: center;
    margin-bottom: var(--spacing-3xl);
  }

  .service-label {
    display: inline-block;
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    color: var(--color-primary);
    letter-spacing: 0.15em;
    margin-bottom: var(--spacing-md);
    position: relative;
    padding-bottom: var(--spacing-sm);
  }

  .service-label::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 40px;
    height: 2px;
    background: var(--color-primary);
  }

  .service-heading {
    font-size: var(--font-size-3xl);
    color: var(--color-text);
    line-height: 1.4;
  }

  .br-sp {
    display: none;
  }

  .service-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-3xl);
  }

  .service-card {
    background: var(--color-bg);
    padding: var(--spacing-xl) var(--spacing-lg);
    border-radius: var(--radius-lg);
    text-align: left;
    border: 1px solid var(--color-border);
    transition: transform var(--transition-base), box-shadow var(--transition-base), border-color var(--transition-base);
  }

  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 24px rgba(0, 123, 167, 0.12);
    border-color: var(--color-primary);
  }

  .service-number {
    display: block;
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-bold);
    color: var(--color-primary);
    margin-bottom: var(--spacing-md);
  }

  .service-title {
    font-size: var(--font-size-lg);
    color: var(--color-text);
    margin-bottom: var(--spacing-sm);
  }

  .service-description {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
    margin: 0;
  }

  .service-cta {
    text-align: center;
    padding: var(--spacing-2xl);
    background: var(--color-bg);
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-border);
  }

  .service-cta-text {
    font-size: var(--font-size-base);
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-lg);
  }

  @media (max-width: 1024px) {
    .service-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .service {
      padding: var(--spacing-3xl) 0;
    }

    .br-sp {
      display: block;
    }

    .service-grid {
      grid-template-columns: 1fr;
    }

    .service-card {
      padding: var(--spacing-lg);
    }
  }
</style>
